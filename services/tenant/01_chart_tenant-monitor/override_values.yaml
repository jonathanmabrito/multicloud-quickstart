global:
  imagePullSecrets: pullsecret
  imageRegistry: $IMAGE_REGISTRY
  pullPolicy: Always

prometheus:
  podMonitor:
    create: "false"
  alertrule:
    create: "false"

fluent:
  rawlogs:
    stdout:
      enable: "false"
  jsonlogs:
    stdout:
      enable: "false"
